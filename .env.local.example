# URL da API backend (configurada em src/environments/environment.ts para dev)
# Em produção, configure no build Angular ou via variável de ambiente do container
API_URL=http://localhost:3001

# Credenciais de teste para o Devin (Computer Use)
TEST_USER_EMAIL=dev@devportal.local
TEST_USER_PASSWORD=DevPortal123!
